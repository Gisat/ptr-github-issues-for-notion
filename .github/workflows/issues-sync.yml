name: Sync GitHub Issues to Notion

on:
  issues:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - name: Generate GitHub App Token
      id: get_token
      uses: actions/create-github-app-token@v3
      with:
        app-id: ${{ secrets.NOTION_SYNC_APP_ID }}
        private-key: ${{ secrets.NOTION_SYNC_APP_KEY }}

    - name: Notion Sync Action
      uses: Gisat/ptr-github-issues-for-notion@wip
      with:
        notion-token: ${{ secrets.NOTION_TOKEN }}
        notion-task-ds: ${{ secrets.NOTION_TASK_DB }}
        notion-project-ds: ${{ secrets.NOTION_PROJECT_DB }}
        notion-users-ds: ${{ secrets.NOTION_USERS_DB }}
        github-token: ${{ steps.get_token.outputs.token }}